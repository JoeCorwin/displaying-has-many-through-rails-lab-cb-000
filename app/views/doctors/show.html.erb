<h1><%=@doctor.name%></h1><br>
<div class="Appts_List">
  <ul>
    <%@doctor.appointments.each do |appt|%>
      <li><%=link_to appt.patient.name, patient_path(appt.patient)%><br>
        <%=appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%></li>
    <%end%>
  </ul>
</div>
